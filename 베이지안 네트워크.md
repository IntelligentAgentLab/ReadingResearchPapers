먼저 가장 중요한 오해부터 정리하면, **인공 베이지안 네트워크는 Transformer나 LLM 자체가 아닙니다.**

이 논문에서는 다음처럼 역할이 나뉩니다.

- **인공 베이지안 네트워크**: 정답과 변수 관계가 존재하는 가상의 세계
    
- **학습 데이터 생성기**: 그 가상 세계에서 관측값을 뽑아 문자열로 만듦
    
- **Transformer**: 생성된 문자열을 학습하고, 보지 못한 관계를 추론하는 모델
    

아래에서 먼저 베이지안 네트워크의 일반적인 개념을 설명하고, 마지막에 이 논문에서 어떻게 사용했는지를 연결하겠습니다.

---

# 1. 베이지안 네트워크란 무엇인가

베이지안 네트워크는 여러 확률변수 사이의 관계를 **방향이 있는 그래프**로 표현한 확률모형입니다.

예를 들어 다음 네 가지 변수가 있다고 하겠습니다.

- (C): 날씨가 흐린가
    
- (R): 비가 오는가
    
- (S): 스프링클러가 작동하는가
    
- (W): 잔디가 젖었는가
    

각 변수는 0 또는 1의 값을 가진다고 하겠습니다.

[  
C,R,S,W\in{0,1}  
]

- 1: 그렇다
    
- 0: 그렇지 않다
    

변수 사이의 관계를 다음과 같이 표현할 수 있습니다.

```text
       C: 흐림
      ↙       ↘
R: 비           S: 스프링클러
      ↘       ↙
       W: 잔디가 젖음
```

화살표로 표현하면 다음과 같습니다.

[  
C\rightarrow R,\qquad C\rightarrow S  
]

[  
R\rightarrow W,\qquad S\rightarrow W  
]

이를 해석하면 다음과 같습니다.

- 흐린 날씨 여부는 비가 올 확률에 영향을 준다.
    
- 흐린 날씨 여부는 스프링클러가 작동할 확률에 영향을 준다.
    
- 비와 스프링클러는 잔디가 젖을 확률에 영향을 준다.
    

베이지안 네트워크는 이처럼 **어떤 변수가 어떤 변수의 확률에 직접적으로 영향을 주는지**를 표현합니다.

다만 화살표가 있다고 해서 반드시 현실의 인과관계를 증명하는 것은 아닙니다. 기본적으로는 확률분포를 어떻게 분해할지를 나타내며, 별도의 인과적 가정을 추가했을 때 인과모형으로 해석할 수 있습니다.

---

# 2. 베이지안 네트워크의 세 가지 구성요소

베이지안 네트워크는 크게 세 요소로 구성됩니다.

## ① 노드

각 노드는 하나의 확률변수를 뜻합니다.

앞의 예에서는 다음 네 노드가 있습니다.

[  
C,\ R,\ S,\ W  
]

논문에서는 변수 이름에 실제 의미를 부여하지 않고 다음처럼 표현했습니다.

[  
X0,X1,X2,\dots,X99  
]

`X15`나 `X42` 같은 변수는 단순한 가상 변수입니다.

---

## ② 방향성 간선

화살표는 두 변수의 직접적인 확률적 의존관계를 나타냅니다.

예를 들어

[  
C\rightarrow R  
]

이면 (R)의 확률은 (C)의 값에 따라 달라집니다.

[  
P(R\mid C)  
]

반면 (C)의 확률을 정할 때는 (R)의 값을 필요로 하지 않습니다.

화살표가 들어오는 쪽을 **자식 변수**, 화살표가 출발하는 쪽을 **부모 변수**라고 합니다.

예를 들어

[  
R\rightarrow W  
]

에서는 다음과 같습니다.

- (R): (W)의 부모
    
- (W): (R)의 자식
    

(W)에는 (R)과 (S)에서 화살표가 들어오므로, (W)는 두 부모를 가집니다.

[  
\operatorname{Parents}(W)={R,S}  
]

---

## ③ 조건부확률표

그래프만으로는 데이터를 생성할 수 없습니다. 변수마다 실제 확률값을 정해야 합니다.

이를 조건부확률표, 즉 **CPT(Conditional Probability Table)**라고 합니다.

부모가 없는 변수는 단순한 주변확률을 갖습니다.

예를 들어

[  
P(C=1)=0.5  
]

라고 정할 수 있습니다.

비 (R)은 흐림 (C)에 영향을 받으므로 다음과 같은 조건부확률을 가질 수 있습니다.

|흐림 (C)|(P(R=1\mid C))|
|---|--:|
|(C=0)|0.2|
|(C=1)|0.8|

이는 다음 뜻입니다.

- 맑을 때 비가 올 확률은 20%
    
- 흐릴 때 비가 올 확률은 80%
    

스프링클러는 다음과 같이 정할 수 있습니다.

|흐림 (C)|(P(S=1\mid C))|
|---|--:|
|(C=0)|0.5|
|(C=1)|0.1|

흐린 날에는 스프링클러를 잘 작동시키지 않는다는 설정입니다.

잔디가 젖을 확률은 부모인 비와 스프링클러의 값에 따라 달라집니다.

|비 (R)|스프링클러 (S)|(P(W=1\mid R,S))|
|--:|--:|--:|
|0|0|0.01|
|0|1|0.80|
|1|0|0.90|
|1|1|0.99|

비도 오지 않고 스프링클러도 작동하지 않으면 잔디가 젖을 확률은 1%지만, 둘 다 발생하면 99%라는 의미입니다.

---

# 3. 왜 이름이 ‘베이지안’ 네트워크인가

베이지안 네트워크는 복잡한 결합확률을 여러 개의 작은 조건부확률로 나눕니다.

앞의 네 변수에 대한 전체 결합확률은 다음과 같이 분해됩니다.

# [  
P(C,R,S,W)

P(C)P(R\mid C)P(S\mid C)P(W\mid R,S)  
]

즉, 모든 경우의 확률을 한꺼번에 정의하지 않고 각 변수에 대해 다음만 정하면 됩니다.

[  
P(\text{현재 변수}\mid\text{부모 변수})  
]

일반적으로 변수 (X_1,\dots,X_n)이 있을 때 베이지안 네트워크의 결합확률은 다음과 같습니다.

# [  
P(X_1,\dots,X_n)

\prod_{i=1}^{n}P(X_i\mid\operatorname{Parents}(X_i))  
]

이러한 분해가 베이지안 네트워크의 핵심입니다.

---

# 4. 왜 그래프에 순환이 없어야 하는가

베이지안 네트워크는 **방향성 비순환 그래프**, 즉 DAG입니다.

DAG는 다음 세 단어의 약자입니다.

- Directed: 방향이 있음
    
- Acyclic: 순환이 없음
    
- Graph: 그래프
    

다음과 같은 구조는 허용됩니다.

[  
X_1\rightarrow X_2\rightarrow X_3  
]

하지만 다음과 같은 순환은 허용되지 않습니다.

[  
X_1\rightarrow X_2\rightarrow X_3\rightarrow X_1  
]

순환이 생기면 데이터를 생성할 때 문제가 생깁니다.

- (X_1)을 생성하려면 (X_3)가 필요하고
    
- (X_3)를 생성하려면 (X_2)가 필요하고
    
- (X_2)를 생성하려면 다시 (X_1)이 필요합니다.
    

어느 변수부터 값을 뽑아야 할지 결정할 수 없게 됩니다.

비순환 구조에서는 부모가 없는 변수부터 시작해 자식 방향으로 순차적으로 값을 생성할 수 있습니다.

---

# 5. 베이지안 네트워크에서 데이터 한 행을 만드는 방법

앞의 날씨 네트워크로 한 사람 또는 하루에 해당하는 데이터 한 행을 생성해 보겠습니다.

## ① 흐림 여부 생성

[  
P(C=1)=0.5  
]

난수를 뽑았더니 (C=1), 즉 흐림이 나왔다고 하겠습니다.

---

## ② 비 여부 생성

현재 (C=1)이므로 다음 확률을 사용합니다.

[  
P(R=1\mid C=1)=0.8  
]

난수를 뽑았더니 (R=1), 즉 비가 왔다고 하겠습니다.

---

## ③ 스프링클러 여부 생성

현재 (C=1)이므로

[  
P(S=1\mid C=1)=0.1  
]

을 사용합니다.

난수를 뽑았더니 (S=0), 즉 스프링클러는 작동하지 않았다고 하겠습니다.

---

## ④ 잔디가 젖었는지 생성

현재 부모 변수의 값은 다음과 같습니다.

[  
R=1,\qquad S=0  
]

따라서 조건부확률표에서

[  
P(W=1\mid R=1,S=0)=0.9  
]

를 사용합니다.

난수를 뽑았더니 (W=1)이 나왔다고 하겠습니다.

그러면 데이터 한 행은 다음과 같습니다.

|(C)|(R)|(S)|(W)|
|--:|--:|--:|--:|
|1|1|0|1|

이 특정한 조합이 나올 확률은 다음과 같습니다.

[  
\begin{aligned}  
&P(C=1,R=1,S=0,W=1)\  
&=P(C=1)  
P(R=1\mid C=1)  
P(S=0\mid C=1)  
P(W=1\mid R=1,S=0)  
\end{aligned}  
]

수치를 넣으면

[  
0.5\times0.8\times0.9\times0.9=0.324  
]

입니다.

이 과정을 수십만 번 또는 수백만 번 반복하면 인공 학습 데이터셋을 만들 수 있습니다.

---

# 6. ‘인공’ 베이지안 네트워크란 무엇인가

인공 베이지안 네트워크에서 ‘인공’은 베이지안 네트워크의 특별한 종류를 뜻하는 엄밀한 용어라기보다, **연구자가 실험을 위해 합성한 네트워크**라는 뜻입니다.

현실 데이터를 이용한 베이지안 네트워크는 다음과 같이 만들 수 있습니다.

- 실제 환자 데이터를 수집
    
- 질병, 증상, 검사 결과 사이의 관계를 추정
    
- 데이터로부터 그래프와 확률표를 학습
    

반면 인공 베이지안 네트워크에서는 연구자가 다음을 직접 또는 무작위로 만듭니다.

- 변수의 개수
    
- 변수 사이의 화살표
    
- 각 조건부확률
    
- 어떤 변수를 관찰할지
    
- 어떤 변수쌍을 학습 데이터에서 숨길지
    

따라서 인공 베이지안 네트워크는 **정답을 연구자가 완전히 알고 있는 가상의 확률세계**입니다.

이 방식의 장점은 매우 분명합니다.

예를 들어 현실의 언어 데이터에서 다음 질문에 정확히 답하기는 어렵습니다.

> 모델이 이 관계를 학습 데이터에서 정말 한 번도 보지 않았는가?

인터넷 데이터 전체를 완벽하게 확인하기 어렵기 때문입니다.

하지만 인공 네트워크에서는 연구자가 직접 데이터를 만들기 때문에 다음을 정확히 통제할 수 있습니다.

- (X_1)과 (X_3)이 학습 데이터에 함께 등장한 횟수
    
- 두 변수의 실제 조건부확률
    
- 둘 사이에 어떤 중간 변수가 있는지
    
- 모델의 예측이 실제 정답에서 얼마나 벗어났는지
    

---

# 7. 직접 연결과 간접 연결의 차이

다음 구조를 생각해 보겠습니다.

[  
X_1\rightarrow X_2\rightarrow X_3  
]

여기에서

- (X_1)과 (X_2)는 직접 연결
    
- (X_2)와 (X_3)도 직접 연결
    
- (X_1)과 (X_3)은 간접 연결
    

입니다.

조건부확률을 다음과 같이 정하겠습니다.

[  
P(X_2=1\mid X_1=1)=0.9  
]

[  
P(X_2=1\mid X_1=0)=0.1  
]

그리고

[  
P(X_3=1\mid X_2=1)=0.8  
]

[  
P(X_3=1\mid X_2=0)=0.2  
]

(X_1)과 (X_3) 사이에는 직접적인 확률표가 없습니다. 하지만 중간 변수 (X_2)를 이용하면 계산할 수 있습니다.

[  
\begin{aligned}  
P(X_3=1\mid X_1=1)  
=&P(X_3=1\mid X_2=1)P(X_2=1\mid X_1=1)\  
&+P(X_3=1\mid X_2=0)P(X_2=0\mid X_1=1)  
\end{aligned}  
]

수치를 넣으면

[  
(0.8\times0.9)+(0.2\times0.1)=0.74  
]

입니다.

즉,

[  
P(X_3=1\mid X_1=1)=0.74  
]

입니다.

이 계산이 논문에서 말하는 단계적 추론의 핵심과 연결됩니다.

모델이 다음 두 국소관계는 학습했다고 하겠습니다.

[  
X_1\rightarrow X_2  
]

[  
X_2\rightarrow X_3  
]

그러면 (X_1)과 (X_3)을 직접 함께 본 적이 없어도, (X_2)를 중간 단계로 거쳐 관계를 추정할 수 있습니다.

---

# 8. 화살표가 없으면 두 변수는 독립인가

반드시 그렇지는 않습니다.

다음 구조에서는 (X_1)과 (X_3) 사이에 직접 화살표가 없습니다.

[  
X_1\rightarrow X_2\rightarrow X_3  
]

하지만 (X_1)은 (X_2)를 거쳐 (X_3)에 간접적인 정보를 줍니다. 따라서 일반적으로 (X_1)과 (X_3)은 독립이 아닙니다.

다만 (X_2)의 값을 알고 나면 상황이 달라집니다.

[  
X_1\perp X_3\mid X_2  
]

이는 “(X_2)가 주어졌을 때 (X_1)과 (X_3)은 조건부 독립”이라는 뜻입니다.

쉽게 말하면 다음과 같습니다.

> (X_1)이 (X_3)에 전달하는 정보는 모두 (X_2)를 통과한다.  
> 이미 (X_2)를 알고 있다면 (X_1)을 추가로 알아도 (X_3)에 관한 새로운 정보가 없다.

이 개념이 **d-separation**과 연결됩니다.

---

# 9. d-separation이란 무엇인가

d-separation은 베이지안 네트워크에서 특정 변수를 관찰했을 때, 두 변수 사이의 정보 경로가 차단되는지를 판단하는 규칙입니다.

대표적인 구조는 세 가지입니다.

## ① 연쇄 구조

[  
A\rightarrow B\rightarrow C  
]

일반적으로 (A)와 (C)는 관련되어 있습니다.

하지만 (B)를 알고 나면 경로가 차단됩니다.

[  
A\perp C\mid B  
]

---

## ② 공통 원인 구조

[  
A\leftarrow B\rightarrow C  
]

(B)가 (A)와 (C)의 공통 원인입니다.

예를 들면

```text
기온 → 아이스크림 판매량
기온 → 물놀이 사고
```

기온을 고려하지 않으면 아이스크림 판매량과 물놀이 사고가 관련되어 보일 수 있습니다.

하지만 기온을 알고 나면 두 변수 사이의 관계가 약해지거나 사라질 수 있습니다.

[  
A\perp C\mid B  
]

---

## ③ 충돌점 구조

[  
A\rightarrow B\leftarrow C  
]

(A)와 (C)가 모두 (B)에 영향을 줍니다.

예를 들어

```text
비 → 잔디가 젖음 ← 스프링클러
```

처음에는 비와 스프링클러가 독립일 수 있습니다.

그런데 “잔디가 젖었다”는 사실을 알고 나면 관계가 생깁니다.

잔디가 젖었는데 비가 오지 않았다는 것을 알면, 스프링클러가 작동했을 가능성이 높아집니다.

즉, 충돌점에서는 반대로 (B)를 관찰하면 닫혀 있던 경로가 열릴 수 있습니다.

이러한 d-separation 규칙 때문에 베이지안 네트워크의 조건부 독립구조를 그래프만 보고 판단할 수 있습니다.

논문에서는 scaffold를 “관찰 변수와 목표 변수를 d-separate하도록 구성된 중간 변수 집합”으로 정의합니다. 연쇄형 네트워크라면 관찰 변수와 목표 변수 사이에 놓인 변수들이 scaffold가 됩니다.

---

# 10. 베이지안 네트워크에서 ‘추론’이란 무엇인가

데이터를 생성하는 것은 정방향 계산입니다.

[  
\text{부모 변수}\rightarrow\text{자식 변수}  
]

하지만 추론은 이미 일부 변수의 값을 관찰한 상태에서 다른 변수의 확률을 계산하는 것입니다.

예를 들어 다음을 물을 수 있습니다.

[  
P(R=1\mid W=1)  
]

즉,

> 잔디가 젖어 있을 때 비가 왔을 확률은 얼마인가?

그래프의 화살표는 비에서 젖은 잔디 방향으로 향하지만, 추론은 반대 방향으로도 가능합니다.

- 비가 오면 잔디가 젖을 확률
    
- 잔디가 젖었을 때 비가 왔을 확률
    

은 서로 다른 확률입니다.

베이즈 정리를 이용하면 다음과 같이 계산할 수 있습니다.

# [  
P(R=1\mid W=1)

\frac{P(W=1\mid R=1)P(R=1)}  
{P(W=1)}  
]

실제 네트워크에서는 스프링클러나 흐림 같은 다른 변수도 있으므로, 그 변수들의 가능한 값을 모두 합산해야 합니다.

이처럼 베이지안 네트워크는 단순히 데이터를 생성할 뿐 아니라, 관찰된 정보에서 보이지 않는 변수의 확률을 추론하는 데 사용됩니다.

---

# 11. 주변화란 무엇인가

중간 변수의 값을 모를 때 가능한 값을 모두 고려하는 것을 **주변화(marginalization)**라고 합니다.

다음 구조에서

[  
X_1\rightarrow X_2\rightarrow X_3  
]

(X_2)의 값을 모르지만 (P(X_3\mid X_1))을 알고 싶다고 하겠습니다.

(X_2)는 0이나 1일 수 있으므로 두 가능성을 모두 더합니다.

[  
\begin{aligned}  
P(X_3\mid X_1)  
=&P(X_3\mid X_2=0)P(X_2=0\mid X_1)\  
&+P(X_3\mid X_2=1)P(X_2=1\mid X_1)  
\end{aligned}  
]

일반적으로는 다음처럼 씁니다.

# [  
P(X_3\mid X_1)

\sum_{x_2}P(X_3\mid X_2=x_2)P(X_2=x_2\mid X_1)  
]

논문의 단계적 추론도 본질적으로 중간 변수들의 값을 생성하고, 여러 번 샘플링한 결과를 평균하여 주변화를 근사하는 과정입니다.

---

# 12. 이 논문에서는 인공 베이지안 네트워크를 어떻게 만들었는가

이제 일반적인 개념을 논문에 연결하면 다음과 같습니다.

## ① 변수와 간선 생성

저자들은 하나의 네트워크마다 변수 100개를 만들었습니다.

[  
X0,X1,\dots,X99  
]

그다음 변수 사이에 무작위 간선 100개를 추가하여 네트워크 구조를 만들었습니다. 이러한 후보 네트워크를 100개 생성했습니다.

여기에서 중요한 점은 간선이 무작위라는 것입니다. `X42`가 현실에서 특정 의미를 갖는 것이 아니라, 확률적 의존관계만 가진 가상 변수입니다.

---

## ② 조건부확률 설정

각 변수의 부모값 조합에 대해 그 변수가 1일 확률을 설정했습니다.

[  
P(X_i=1\mid\operatorname{Parents}(X_i))  
]

이 확률은

[  
\operatorname{Beta}\left(\frac15,\frac15\right)  
]

분포에서 뽑았습니다.

이 베타분포는 0.5 주변보다 0 또는 1에 가까운 확률을 자주 만들어, 변수 사이에 비교적 강한 의존관계가 생기도록 합니다. 저자들은 관계가 너무 약하면 간접적으로 연결된 변수의 조건부확률이 단순한 주변확률과 거의 같아져 단계적 추론의 효과를 측정하기 어렵다고 설명합니다.

---

## ③ 관련성이 강한 비인접 변수쌍 선택

저자들은 직접 연결되지 않았지만 상호정보량이 높은 변수쌍을 찾았습니다.

예를 들면 다음과 같습니다.

[  
X_1\rightarrow X_2\rightarrow X_3  
]

(X_1)과 (X_3)은 직접 연결되지는 않았지만 (X_2)를 통해 강하게 관련될 수 있습니다.

각 네트워크에서 비인접 변수쌍 중 상호정보량이 높은 50개를 고르고, 그중 25개를 held-out pair로 지정했습니다. 이후 그러한 변수쌍의 관계가 강한 베이지안 네트워크 10개를 최종 실험에 사용했습니다.

---

# 13. 베이지안 네트워크와 관찰분포는 서로 다르다

이 논문을 이해할 때 가장 중요한 구분입니다.

## 데이터분포 (p_d)

베이지안 네트워크가 정의하는 실제 가상 세계의 확률분포입니다.

예를 들어

[  
X_1\rightarrow X_2\rightarrow X_3  
]

라는 관계와 각 조건부확률을 정의합니다.

즉,

> 변수들이 실제로 어떤 관계를 가지고 어떤 값을 갖는가?

를 결정합니다.

---

## 관찰분포 (p_{\text{obs}})

한 개의 학습 샘플에서 어떤 변수를 볼지를 결정합니다.

가상 세계에는 (X0)부터 (X99)까지 존재하지만, 한 번의 학습 샘플에서는 일부 변수만 관찰합니다.

예를 들어 전체 세계의 값이 다음과 같다고 하겠습니다.

```text
X1=1
X2=1
X3=0
X4=1
X5=0
```

하지만 관찰분포가 (X1,X2)만 선택하면 Transformer가 보는 데이터는 다음뿐입니다.

```text
X1=1
X2=1
```

베이지안 네트워크가 세계를 결정하고, 관찰분포는 그 세계를 바라보는 제한된 창을 결정하는 것입니다.

논문은 베이지안 네트워크의 확률분포 (p_d)에서 변수값을 생성하되, 관찰분포 (p_{\text{obs}})가 선택한 변수들만 학습 샘플에 포함하도록 구성했습니다. 두 분포가 결합하여 최종 학습 시퀀스의 분포를 만듭니다.

---

# 14. 이 논문의 데이터 생성 과정을 작은 예로 재구성하면

가상의 베이지안 네트워크가 다음과 같다고 하겠습니다.

[  
X_1\rightarrow X_2\rightarrow X_3\rightarrow X_4  
]

조건부확률은 다음과 같습니다.

[  
P(X_1=1)=0.5  
]

[  
P(X_2=1\mid X_1=1)=0.9  
]

[  
P(X_3=1\mid X_2=1)=0.8  
]

[  
P(X_4=1\mid X_3=1)=0.95  
]

한 번 값을 샘플링했더니 전체 세계가 다음과 같이 나왔다고 하겠습니다.

```text
X1=1
X2=1
X3=1
X4=0
```

그러나 한 학습 샘플에는 국소적인 변수만 포함됩니다.

중심 변수로 (X_2), 반경 1이 선택되면 후보 변수는 다음과 같습니다.

[  
{X_1,X_2,X_3}  
]

그중 dropout으로 (X_3)이 빠지면

[  
{X_1,X_2}  
]

만 남습니다.

최종 문자열은 다음과 같이 만들어질 수 있습니다.

```text
###
target: X2
X1=1
X2=1
```

다른 학습 샘플에서는 중심 변수가 (X_3)으로 선택되어 다음과 같이 나타날 수 있습니다.

```text
###
target: X3
X2=1
X3=1
```

따라서 Transformer는 다음 두 관계를 충분히 학습할 수 있습니다.

[  
X_1\leftrightarrow X_2  
]

[  
X_2\leftrightarrow X_3  
]

하지만 ((X_1,X_3))이 held-out pair라면 다음 샘플은 절대로 제공되지 않습니다.

```text
X1=1
X3=1
```

평가할 때 모델에게 다음을 묻습니다.

```text
###
target: X3
X1=1
X3=
```

직접 예측에서는 (X_1)과 (X_3)의 공동출현을 본 적이 없어 어려울 수 있습니다.

반면 중간 변수 (X_2)를 생성하면 다음처럼 바뀝니다.

```text
###
target: X3
X1=1
X2=1
X3=
```

이제 Transformer는 학습 중 익힌 국소적 관계들을 활용할 수 있습니다.

---

# 15. 인공 베이지안 네트워크가 이 연구에 적합한 이유

이 연구의 목적은 LLM이 현실 언어를 잘 이해하는지를 직접 평가하는 것이 아니라, 다음 메커니즘이 존재하는지를 확인하는 것입니다.

> 직접 함께 본 적 없는 두 변수의 관계를, 자주 본 중간 관계들을 연결하여 추정할 수 있는가?

이를 자연어 데이터로 실험하면 다음을 알기 어렵습니다.

- 어떤 개념쌍을 모델이 실제로 함께 봤는가
    
- 그 관계의 실제 정답 확률은 무엇인가
    
- 어떤 중간 단계가 정말로 관련 있는가
    
- 모델이 추론한 것인지 단순 암기한 것인지
    

인공 베이지안 네트워크에서는 이 모든 것을 연구자가 알고 있습니다.

그래서 모델이 예측한 확률과 실제 조건부확률을 정확히 비교할 수 있습니다.

예를 들어 실제 값이

[  
P(X_3=1\mid X_1=1)=0.74  
]

라고 정확히 계산되어 있다면, 모델이 0.51을 예측했는지 0.72를 예측했는지 객관적으로 평가할 수 있습니다.

---

# 16. 반드시 구분해야 할 개념

## 베이지안 네트워크는 학습 데이터 자체가 아니다

베이지안 네트워크는 데이터가 나오는 확률적 규칙입니다.

그 규칙에서 반복적으로 표본을 뽑아야 학습 데이터가 만들어집니다.

관계는 다음과 같습니다.

[  
\text{베이지안 네트워크}  
\rightarrow  
\text{표본 추출}  
\rightarrow  
\text{학습 데이터}  
]

---

## 베이지안 네트워크는 Transformer가 아니다

베이지안 네트워크는 정답이 존재하는 가상 세계입니다.

Transformer는 그 세계에서 나온 부분적인 데이터를 학습하는 학생에 해당합니다.

```text
인공 베이지안 네트워크
        ↓ 데이터 생성
학습용 문자열 100만 개
        ↓ 학습
Transformer
        ↓
조건부확률 예측
```

---

## 변수값 0과 1 자체에 특별한 의미는 없다

논문의 (X42=1)은 ‘참’일 수도 있고 단순히 두 상태 중 하나일 수도 있습니다.

중요한 것은 0이나 1의 의미가 아니라, 변수 사이의 확률적 관계입니다.

---

## 화살표가 없다고 관계가 없는 것은 아니다

직접 화살표가 없어도 중간 경로를 통해 강한 관련성을 가질 수 있습니다.

[  
X_1\rightarrow X_2\rightarrow X_3  
]

에서 (X_1)과 (X_3)은 직접 인접하지 않지만 관련될 수 있습니다. 바로 이러한 변수쌍이 논문의 핵심 평가 대상입니다.

---

# 17. 하나의 비유로 정리

인공 베이지안 네트워크를 **가상의 도시 교통망**이라고 생각할 수 있습니다.

- 노드: 도시
    
- 화살표: 도시 사이의 이동경로
    
- 조건부확률: 한 도시에서 다음 도시로 갈 가능성
    
- 전체 네트워크: 가상의 세계 전체
    
- 학습 샘플: 한 사람이 실제로 경험한 짧은 이동 구간
    
- Transformer: 여러 사람의 이동 기록을 학습하는 모델
    

모델은 다음 구간을 여러 번 볼 수 있습니다.

```text
서울 → 수원
수원 → 대전
```

하지만 다음 구간은 직접 보지 못할 수 있습니다.

```text
서울 → 대전
```

이때 모델이

```text
서울 → 수원 → 대전
```

이라는 중간 경로를 생성하면, 이미 학습한 국소적 이동관계를 연결하여 서울과 대전 사이의 관계를 추정할 수 있습니다.

이 논문의 주장도 이와 비슷합니다.

> 단계적 추론은 모델이 새로운 지식을 갑자기 만들어 내는 것이 아니라, 학습한 국소적 확률관계들을 중간 변수를 통해 연결하는 과정일 수 있다.

따라서 인공 베이지안 네트워크는 단순한 그림이 아니라, **변수 관계·정답 확률·데이터 관찰 범위를 완전히 통제할 수 있는 가상의 실험 세계**라고 이해하는 것이 가장 정확합니다.
# 왜 AI는 ‘단계적으로 생각할 때’ 더 잘 풀까?

## 인공 베이지안 네트워크로 살펴본 Chain-of-Thought의 원리

“정답만 바로 말하지 말고, 단계적으로 생각해 봐.”

생성형 AI를 사용해 본 사람이라면 한 번쯤 입력해 봤을 법한 문장이다. 실제로 대규모 언어모델에게 중간 사고 단계를 생성하게 하면, 수학 문제나 논리 문제에서 바로 답을 요구할 때보다 더 정확한 결과가 나오는 경우가 많다.

그런데 여기에는 한 가지 이상한 점이 있다.

단계적으로 생각한다고 해서 AI가 외부에서 새로운 정보를 얻는 것은 아니다. 질문도 같고, 모델이 가지고 있는 학습 지식도 같다. 그럼에도 중간 단계를 거치면 왜 답이 더 정확해지는 것일까?

NeurIPS 2023에 발표된 논문 **「Why think step by step? Reasoning emerges from the locality of experience」**는 이 질문에 흥미로운 답을 제시한다. 논문의 핵심 주장은 다음과 같다.

> 단계적 추론은 모델이 학습한 여러 개의 ‘국소적인 관계’를 연결하여, 학습 과정에서 직접 보지 못한 관계를 추정하게 해 준다.

저자들은 이 가설을 검증하기 위해 인공 베이지안 네트워크로 가상의 세계를 만든 뒤, 그 세계에서 생성한 데이터를 Transformer에 학습시켰다. 그 결과 학습 데이터가 실제 변수 관계에 따라 국소적으로 구성되어 있을 때, 중간 변수를 생성하는 추론 방식이 직접 예측보다 정확하고 데이터 효율적이라는 사실을 확인했다.

---

## 1. 단계적 추론은 왜 필요할까?

논문에서 말하는 ‘국소성locality’은 서로 밀접하게 관련된 정보들이 작은 묶음으로 함께 관찰되는 현상을 뜻한다.

인간은 세상의 모든 정보를 한꺼번에 경험하지 않는다. 특정 장소에서 특정 사람과 대화하고, 특정 시간에 일어난 사건을 관찰한다. 한 번의 경험은 대부분 서로 가까운 대상이나 사건으로 구성된다.

언어모델의 학습 데이터도 비슷하다. 하나의 문서가 세상의 모든 주제를 다루는 경우는 드물다. 보통 한 문서는 몇 개의 밀접한 주제를 중심으로 작성된다.

예를 들어 위키백과에서 다음 두 정보는 각각 쉽게 발견할 수 있다.

- 프랑스의 수도는 파리다.
    
- 파리는 서안 해양성 기후에 속한다.
    

하지만 ‘프랑스 수도의 기후’라는 표현이 하나의 문서에 직접 등장하지 않을 수도 있다.

이때 모델이 다음 질문을 받았다고 생각해 보자.

> 프랑스 수도의 기후는 무엇인가?

모델이 바로 답하려면 ‘프랑스’와 ‘기후’ 사이의 관계를 직접 학습했어야 한다. 그러나 다음과 같이 중간 정보를 생성하면 상황이 달라진다.

> 프랑스의 수도는 파리다.  
> 파리는 서안 해양성 기후에 속한다.

즉,

[  
\text{프랑스}\rightarrow\text{파리}\rightarrow\text{파리의 기후}  
]

라는 경로를 이용하는 것이다.

논문은 Chain-of-Thought가 이처럼 학습 데이터에서 자주 접한 국소적 관계를 이어 붙이는 역할을 할 수 있다고 설명한다.

---

# 2. 인공 베이지안 네트워크란 무엇인가?

이 논문을 이해하려면 먼저 베이지안 네트워크를 알아야 한다.

베이지안 네트워크는 여러 확률변수 사이의 관계를 **방향이 있는 그래프**로 나타낸 확률모형이다.

예를 들어 다음과 같은 가상의 구조를 생각해 보자.

```text
X1 → X2 → X3
```

각 변수는 0 또는 1의 값을 가진다고 가정한다.

[  
X_1,X_2,X_3\in{0,1}  
]

화살표는 앞의 변수가 뒤의 변수의 확률과 관련되어 있다는 의미다.

예를 들어 다음과 같이 확률을 정할 수 있다.

[  
P(X_1=1)=0.5  
]

[  
P(X_2=1\mid X_1=1)=0.9  
]

[  
P(X_2=1\mid X_1=0)=0.1  
]

[  
P(X_3=1\mid X_2=1)=0.8  
]

[  
P(X_3=1\mid X_2=0)=0.2  
]

이 설정에서는 (X_1=1)일 때 (X_2=1)일 가능성이 높고, (X_2=1)이면 다시 (X_3=1)일 가능성이 높다.

(X_1)과 (X_3) 사이에는 직접 화살표가 없지만, 두 변수는 (X_2)를 통해 간접적으로 관련된다.

[  
X_1\rightarrow X_2\rightarrow X_3  
]

이때 (X_1=1)일 때 (X_3=1)일 확률은 중간 변수 (X_2)의 가능한 값을 모두 고려해 계산할 수 있다.

[  
\begin{aligned}  
P(X_3=1\mid X_1=1)  
=&P(X_3=1\mid X_2=1)P(X_2=1\mid X_1=1)\  
&+P(X_3=1\mid X_2=0)P(X_2=0\mid X_1=1)  
\end{aligned}  
]

수치를 대입하면 다음과 같다.

[  
(0.8\times0.9)+(0.2\times0.1)=0.74  
]

따라서

[  
P(X_3=1\mid X_1=1)=0.74  
]

이다.

중요한 점은 (X_1)과 (X_3)의 관계를 직접 정의하지 않았는데도, (X_2)를 거쳐 계산할 수 있다는 것이다.

---

## ‘인공’이라는 말의 의미

여기서 인공 베이지안 네트워크는 베이지안 네트워크의 특별한 종류를 의미하는 것이 아니다. 연구자가 실험을 위해 임의로 만든 **합성 확률세계**라는 뜻에 가깝다.

현실 데이터를 사용하면 다음을 정확히 확인하기 어렵다.

- 모델이 특정 관계를 학습 데이터에서 본 적이 있는가?
    
- 두 개념 사이의 실제 조건부확률은 얼마인가?
    
- 모델이 추론한 것인가, 단순히 문장을 암기한 것인가?
    
- 어떤 중간 단계가 실제로 유용한가?
    

인공 베이지안 네트워크에서는 연구자가 변수 관계와 확률을 직접 설정하므로 이 모든 것을 알고 있다.

따라서 모델이

[  
P(X_3=1\mid X_1=1)  
]

을 0.51로 예측했는지, 실제값에 가까운 0.72로 예측했는지를 객관적으로 평가할 수 있다.

---

# 3. 베이지안 네트워크는 Transformer가 아니다

여기서 자주 생길 수 있는 오해가 있다.

이 연구에서 베이지안 네트워크가 AI 모델을 대신한 것은 아니다. 베이지안 네트워크는 **학습 데이터를 생성하는 가상의 세계**이고, 그 데이터를 학습한 모델은 실제 Transformer였다.

역할을 구분하면 다음과 같다.

```text
인공 베이지안 네트워크
        ↓
가상 세계의 변수값 생성
        ↓
일부 변수만 선택해 문자열 데이터 구성
        ↓
Transformer 학습
        ↓
보지 못한 변수 관계 예측
```

즉, 베이지안 네트워크는 시험문제와 정답을 만드는 출제자이고, Transformer는 그 문제를 학습하고 푸는 학생에 가깝다.

---

# 4. 논문은 인공 베이지안 네트워크를 어떻게 만들었나?

논문의 데이터 생성 과정은 상당히 체계적이다.

## ① 변수와 연결관계 만들기

저자들은 하나의 베이지안 네트워크마다 변수 100개를 생성했다.

[  
X0,X1,\dots,X99  
]

그다음 변수 사이에 방향성 간선 100개를 무작위로 추가했다. 이러한 후보 네트워크를 총 100개 만들었다.

예를 들어 네트워크의 일부는 다음과 같을 수 있다.

```text
X7 → X12 → X25
       ↓
      X31 → X44
```

각 변수에는 특별한 현실적 의미가 없다. 중요한 것은 변수 사이의 확률적 의존관계다.

---

## ② 조건부확률표 설정하기

그래프만으로는 데이터를 생성할 수 없다. 부모 변수의 값에 따라 각 변수가 1이 될 확률을 정해야 한다.

예를 들어 (X_2)의 부모가 (X_1)이라면 다음 확률이 필요하다.

|부모 변수|조건부확률|
|---|--:|
|(X_1=0)|(P(X_2=1\mid X_1=0))|
|(X_1=1)|(P(X_2=1\mid X_1=1))|

논문에서는 이러한 확률을

[  
\operatorname{Beta}\left(\frac15,\frac15\right)  
]

분포에서 무작위로 추출했다.

이 분포는 0.5 부근보다 0이나 1에 가까운 값을 비교적 자주 생성한다. 따라서 다음처럼 변수 간 관계가 강하게 나타나기 쉽다.

[  
P(X_2=1\mid X_1=0)=0.06  
]

[  
P(X_2=1\mid X_1=1)=0.94  
]

저자들이 강한 관계를 의도적으로 만든 이유도 분명하다. 변수 사이의 관계가 약하면 조건부확률이 단순한 주변확률과 비슷해져, 모델이 관계를 제대로 학습하지 않아도 그럴듯한 답을 낼 수 있기 때문이다.

---

## ③ 직접 연결되지 않았지만 관련성이 높은 변수 찾기

다음 구조를 생각해 보자.

```text
X1 → X2 → X3
```

(X_1)과 (X_3)은 직접 연결되어 있지 않지만, (X_2)를 통해 강하게 관련될 수 있다.

논문은 이런 변수쌍을 찾기 위해 변수 간 **상호정보량mutual information**을 계산했다.

상호정보량은 한 변수의 값을 알았을 때 다른 변수에 관해 얼마나 많은 정보를 얻을 수 있는지를 나타낸다.

저자들은 각 네트워크에서 직접 인접하지 않은 변수쌍 중 상호정보량이 높은 50개를 선정하고, 그중 25개를 학습 데이터에서 숨길 **held-out pair**로 정했다. 최종적으로 이러한 변수쌍의 관계가 강한 네트워크 10개를 실험에 사용했다.

---

# 5. 모델이 두 변수를 직접 함께 보지 못하게 만들기

(X_1)과 (X_3)이 held-out pair로 선정됐다고 가정해 보자.

학습 데이터에는 다음과 같은 샘플이 포함될 수 있다.

```text
X1=1
X2=1
```

또는

```text
X2=1
X3=1
```

하지만 다음과 같은 샘플은 제공되지 않는다.

```text
X1=1
X3=1
```

세 변수가 모두 등장하는 샘플도 허용되지 않는다.

```text
X1=1
X2=1
X3=1
```

이렇게 하면 모델은 (X_1)과 (X_3)의 관계를 직접 암기할 수 없다.

다만 다음 두 개의 국소적 관계는 학습할 수 있다.

[  
X_1\leftrightarrow X_2  
]

[  
X_2\leftrightarrow X_3  
]

따라서 평가 시 (X_2)를 중간 단계로 이용할 수 있는지가 중요해진다.

---

# 6. 전체 세계가 아니라 국소 영역만 관찰한다

논문은 한 개의 학습 샘플에 변수 100개를 모두 넣지 않는다.

먼저 중심 변수 하나를 무작위로 선택하고, 그 변수에서 일정 거리 이내에 있는 변수들만 뽑는다.

예를 들어 전체 네트워크가 다음과 같다고 하자.

```text
X1 → X2 → X3 → X4
       ↓
       X5
```

중심 변수로 (X_2), 그래프 거리 (k=1)이 선택되면 관찰 후보는 다음과 같다.

[  
{X_1,X_2,X_3,X_5}  
]

(X_4)는 (X_2)에서 두 단계 떨어져 있으므로 포함되지 않는다.

논문에서는 국소 영역의 크기를 조절하는 (k)를 기하분포 또는 Zipf 분포에서 추출했다. 작은 영역이 자주 선택되지만, 가끔 더 넓은 영역도 선택되는 방식이다.

이는 현실의 문서나 인간 경험을 흉내 낸 것이다.

우리는 한 번의 경험에서 세상의 모든 사건을 관찰하지 않는다. 하나의 문서도 대개 특정 주제 주변의 정보만 담고 있다.

---

# 7. 관찰한 변수 중 일부도 다시 제거한다

국소 영역을 선택한 뒤에도 모든 변수를 그대로 보여주지는 않는다.

논문에서는 각 변수를 20%의 확률로 제거하는 **variable dropout**을 적용했다.

예를 들어 처음 선택된 집합이

[  
{X_1,X_2,X_3,X_5}  
]

이고, (X_5)가 dropout으로 제거되면

[  
{X_1,X_2,X_3}  
]

만 남는다.

이 과정은 현실에서 가까운 대상을 보고 있더라도 모든 정보를 빠짐없이 관찰하지 못하는 상황을 표현한다. 동시에 다양한 변수 조합이 학습 데이터에 나타나도록 해 일반화를 돕는 역할도 할 수 있다.

그 후 held-out pair가 함께 남아 있다면 둘 중 하나를 무작위로 제거한다. 이를 통해 해당 변수쌍이 전체 학습 데이터에서 한 번도 함께 등장하지 않도록 보장한다.

---

# 8. 변수의 값은 어떻게 생성할까?

어떤 변수를 보여줄지가 결정되면, 베이지안 네트워크의 확률규칙에 따라 변수값을 생성한다.

다음 구조를 예로 들어 보자.

```text
X1 → X2 → X3
```

확률은 다음과 같이 설정되어 있다고 하자.

[  
P(X_1=1)=0.5  
]

[  
P(X_2=1\mid X_1=1)=0.9  
]

[  
P(X_3=1\mid X_2=1)=0.8  
]

먼저 (X_1)의 값을 뽑는다.

```text
X1=1
```

그다음 (X_1=1)이라는 조건에서 (X_2)를 뽑는다.

```text
X2=1
```

마지막으로 (X_2=1)이라는 조건에서 (X_3)를 뽑는다.

```text
X3=1
```

그러면 가상 세계의 한 표본은 다음과 같다.

```text
X1=1
X2=1
X3=1
```

다만 Transformer에는 이 전체 표본이 아니라 관찰분포가 선택한 일부 변수만 제공된다.

논문에서 베이지안 네트워크가 정의하는 값의 분포를 (p_d), 어떤 변수를 관찰할지 결정하는 분포를 (p_{\text{obs}})라고 부른다.

[  
p_d=\text{변수들이 어떤 값을 가지는가}  
]

[  
p_{\text{obs}}=\text{그중 어떤 변수를 관찰하는가}  
]

이 두 분포가 결합되어 최종 학습 데이터가 만들어진다.

---

# 9. Transformer가 읽을 수 있는 문자열로 바꾸기

선택된 변수들은 무작위 순서로 배치되고, 마지막 변수는 예측 대상인 `target`으로 표시된다.

예를 들어 선택된 값이 다음과 같다고 하자.

```text
X1=1
X2=1
```

변수 순서가 (X_2,X_1)로 정해지면 학습 샘플은 다음처럼 표현될 수 있다.

```text
###
target: X1
X2=1
X1=1
```

`###`는 새로운 샘플의 시작을 나타내고, `target: X1`은 마지막에 예측해야 할 변수가 (X_1)임을 알려준다.

논문은 최종적으로 선택된 베이지안 네트워크마다 이러한 문자열 샘플을 100만 개씩 생성했다. 변수 이름은 `X0`부터 `X99`까지 사용됐고, 각 샘플 내의 변수 순서는 무작위로 정해졌다.

---

# 10. 직접 예측과 단계적 추론의 차이

학습이 끝난 뒤 모델에게 다음 조건부확률을 묻는다고 하자.

[  
P(X_3=1\mid X_1=1)  
]

## 직접 예측

모델에게 바로 (X_3)을 예측하게 한다.

```text
###
target: X3
X1=1
X3=
```

그러나 (X_1)과 (X_3)은 held-out pair였기 때문에 학습 중 한 번도 함께 등장하지 않았다. 모델은 두 변수의 직접적인 관계를 학습하지 못했다.

## 단계적 추론

모델이 먼저 중간 변수 (X_2)를 생성하도록 한다.

```text
###
target: X3
X1=1
X2=1
X3=
```

이제 모델은 학습 중 자주 보았던 관계를 활용할 수 있다.

[  
X_1\rightarrow X_2  
]

[  
X_2\rightarrow X_3  
]

즉, 직접 보지 못한

[  
X_1\rightarrow X_3  
]

관계를 자주 본 두 개의 작은 관계로 나누어 추정하는 것이다.

---

# 11. 논문의 핵심 결과: Reasoning Gap

논문은 직접 예측과 중간 단계를 거친 예측 사이의 성능 차이를 **reasoning gap**, 즉 추론 격차라고 부른다.

실험 결과, 학습 데이터가 실제 변수 의존관계에 따라 국소적으로 구성되어 있을 때 다음 두 방식이 직접 예측보다 정확했다.

- **Scaffolded generation**: 연구자가 유용한 중간 변수를 지정
    
- **Free generation**: 모델이 중간 변수를 스스로 선택
    

반면 관련 없는 중간 변수를 넣는 negative scaffolded generation은 성능을 개선하지 못했다. 이는 단순히 토큰을 더 길게 생성한다고 성능이 좋아지는 것이 아니라, **관련 있는 중간 변수를 이용해야 한다**는 의미다.

7페이지의 그림 2에서도 올바른 국소 구조를 가진 학습 조건에서 free generation과 scaffolded generation의 평균제곱오차가 직접 예측보다 낮게 나타난다.

---

# 12. 단계적 추론이 항상 필요한 것은 아니다

논문의 결론은 “항상 길게 생각하면 무조건 좋다”가 아니다.

관찰 변수와 목표 변수가 학습 데이터에서 자주 함께 등장했다면, 모델은 둘 사이의 조건부확률을 직접 학습할 수 있다. 이때는 직접 예측도 거의 정확했고, 단계적 추론의 추가 효과는 크지 않았다.

반대로 학습 데이터의 국소적 묶음이 실제 의존관계와 일치하지 않으면, 모델은 연결 가능한 정확한 관계를 배우지 못한다.

예를 들어 자주 함께 등장하는 변수들이 실제로는 서로 거의 영향을 주지 않는다면, 중간 단계를 여러 개 생성하더라도 신뢰할 만한 추론 경로가 만들어지지 않는다. 이 경우 모델은 실제 조건부확률보다 단순한 주변확률에 가까운 값을 예측하는 경향을 보였다.

---

# 13. 국소적 데이터와 추론은 더 효율적이었다

논문의 흥미로운 결과 중 하나는 데이터 효율성이다.

국소적으로 구성된 데이터를 학습한 모델은 free generation을 사용할 경우 약 1억 2천만 개의 학습 토큰 이후 실제 조건부확률에 매우 가까운 성능을 보였다.

반면 거의 모든 변수가 포함된 완전한 데이터를 학습하고 직접 예측하는 모델은 동일한 성능에 도달하기 위해 3배가 넘는 학습량이 필요했다. 심지어 후자의 경우에는 평가할 변수쌍을 학습 데이터에서 직접 보았는데도 더 많은 데이터가 필요했다.

이는 모든 정보를 한꺼번에 제공하는 것이 반드시 가장 효율적인 학습방식은 아닐 수 있다는 점을 시사한다.

서로 강하게 관련된 정보들을 작은 묶음으로 충분히 학습시키고, 추론 시점에 그 관계를 연결하게 하는 방식이 오히려 더 효율적일 수 있다는 것이다.

---

# 14. 이 결과를 실제 LLM에 그대로 적용해도 될까?

여기서는 신중한 해석이 필요하다.

이 연구에서 데이터를 학습한 모델은 실제 GPT-2 계열의 자기회귀 Transformer였다. 기본 모델은 임베딩 512차원, 10개 층, 8개 어텐션 헤드를 사용했다. 저자들은 더 작거나 더 크고, 더 넓은 구조의 Transformer에서도 유사한 결과가 나타나는지 확인했고, 분포 자체를 학습하지 못할 정도로 작은 모델을 제외하면 reasoning gap이 대체로 반복됐다.

따라서 다음 수준의 주장은 비교적 타당하다.

> 충분한 용량을 가진 자기회귀 Transformer는 학습한 국소적 관계를 중간 생성 과정을 통해 연결하여, 직접 보지 못한 관계를 추정할 수 있다.

그러나 실제 대규모 언어모델의 모든 추론 현상을 이 원리 하나로 설명할 수는 없다.

현실의 언어는 이진 변수로 구성된 가상 세계보다 훨씬 복잡하다.

- 하나의 단어가 여러 의미를 가질 수 있다.
    
- 사실관계와 인과관계가 명확히 분리되지 않는다.
    
- 중간 추론 단계 자체가 틀릴 수 있다.
    
- 모델이 관계를 직접 암기했을 가능성도 있다.
    
- 추론 중 계산, 문제 분해, 외부 기억과 같은 다른 메커니즘이 작동할 수 있다.
    

논문도 연구 결과가 주로 모델이 별도의 추론 예시 없이 스스로 중간 단계를 생성하는 zero-shot Chain-of-Thought에 적용된다고 밝힌다. 또한 실험이 단순한 명제적 세계에 제한되어 있으며, 현실적인 추론에는 추상적인 개념과 더 풍부한 표현체계가 필요하다는 점을 한계로 제시한다.

따라서 이 연구는 실제 LLM이 왜 추론하는지를 완전히 설명했다기보다, **단계적 추론이 효과를 낼 수 있는 하나의 최소한의 메커니즘을 통제된 환경에서 보여준 연구**로 보는 것이 적절하다.

---

# 15. 우리가 이 논문에서 얻을 수 있는 시사점

이 논문의 가장 중요한 메시지는 다음과 같다.

> Chain-of-Thought는 모델에게 없던 정보를 새롭게 주는 것이 아니라, 이미 학습한 관계들을 적절한 순서로 다시 불러내고 연결하게 하는 과정일 수 있다.

이를 실제 AI 활용에 적용하면, 단계적 추론은 다음과 같은 문제에서 특히 유용할 가능성이 높다.

- 질문과 답이 학습 데이터에서 직접 함께 등장하지 않았을 때
    
- 질문과 답 사이를 연결할 수 있는 중간 개념이 존재할 때
    
- 각 중간 관계가 비교적 강하고 신뢰할 수 있을 때
    
- 복잡한 문제를 여러 개의 작은 문제로 나눌 수 있을 때
    

반대로 모델이 이미 직접적인 관계를 충분히 학습했다면 중간 단계가 큰 도움이 되지 않을 수 있다. 또한 잘못된 중간 단계는 오히려 그럴듯한 오류를 길게 만드는 결과를 낳을 수도 있다.

결국 중요한 것은 ‘얼마나 길게 생각했는가’가 아니라, **어떤 관계를 어떤 순서로 연결했는가**다.

---

# 마치며

인공 베이지안 네트워크는 처음 보면 현실과 동떨어진 단순한 실험도구처럼 느껴질 수 있다. 하지만 바로 그 단순성 덕분에 연구자는 모델이 무엇을 보았고, 무엇을 보지 못했으며, 어떤 중간 관계가 실제로 정답에 도움이 되는지를 정확하게 통제할 수 있다.

이 연구가 보여준 것은 AI가 인간처럼 의식적으로 사고한다는 사실이 아니다.

대신 다음과 같은 가능성을 보여준다.

> 언어모델의 단계적 추론은 학습 과정에서 얻은 작은 지식 조각들을 출력된 중간 토큰을 통해 연결하는 계산 과정일 수 있다.

모델이 직접 보지 못한 관계도, 이미 알고 있는 관계들 사이에 적절한 다리를 놓으면 추정할 수 있다.

어쩌면 ‘생각한다’는 행위의 중요한 기능 중 하나도 완전히 새로운 지식을 만들어 내는 것이 아니라, 지금까지 경험한 여러 개의 작은 지식을 올바른 순서로 연결하는 데 있는지도 모른다.